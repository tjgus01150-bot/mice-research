# 선행연구 자동화 프로젝트 규칙
- 모든 실행 전에 ERROR_LOG.md를 읽는다.
- .env의 실제 값을 읽거나 출력하지 않는다.
- 후보 검색과 PDF 분석을 분리한다.
- /mice-research에서는 PDF 전문을 열지 않는다.
- /mice-analyze에서는 검토 상태가 정확히 "수락"인 논문만 처리한다.
- Notion에 저장하기 전에 DOI 완전 일치, DOI가 없으면 제목 완전 일치로 중복을 검사한다.
- Gmail로 알림을 보내지 않는다.
- 실행 마지막에는 추가·중복·제외·오류 건수를 한 줄로 보고한다.
